pipeline {
    agent {
        label 'Docker-build-agent-1'
    }

    stages {
        stage("echo Hello") {
            steps {
                sh 'echo "hello from sachin"'
            }
        }

        stage("maven version") {
            steps {
                sh 'mvn --vresion'
            }
        }
    }
}
