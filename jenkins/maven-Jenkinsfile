pipeline {
    agent {
        label 'agent1'
    }

    stages {
        stage("echo Hello") {
            steps {
                sh 'hello from sachin'
            }
        }

        stage("maven version") {
            steps {
                sh 'mvn --vresion'
            }
        }
    }
}
