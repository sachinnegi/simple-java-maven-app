pipeline {
    agent {
        label: 'agent1'
    }

    stages {
        stage("echo Hello") {
            sh 'hello from sachin'
        }

        stage("maven version") {
            sh 'mvn --vresion'
        }
    }
}
